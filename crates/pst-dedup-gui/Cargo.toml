[package]
name = "pst-dedup-gui"
version = "0.1.0"
edition = "2021"
description = "PST email deduplication tool — egui desktop application"
license = "LicenseRef-Proprietary"
license-file = "../../LICENSE"

[dependencies]
pst-reader = { path = "../pst-reader" }
dedup-engine = { path = "../dedup-engine" }
# In-process unique-pst orchestration (0072) — same pipeline as CLI, no process spawn.
pst-dedup-cli = { path = "../pst-dedup-cli" }
eframe = { workspace = true }
rfd = { workspace = true }
tracing = { workspace = true }
tracing-subscriber = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }
