/target/
.ledgerful/
.env
*.log
# Root-level agent scratch only (track DoD reviews live under conductor/ and are force-added).
/review.md
.claude/
*.html
# Synthetic Teams HTML fixtures (track 0055) must be tracked for CI/tests.
!fixtures/teams/
!fixtures/teams/*.html

# Local scratch / generated
output/

# Conductor track docs: local by default. Already-tracked board/track files stay
# versioned; new DoD artifacts (plan/spec/review/baseline/implementation-notes)
# must be `git add -f` (same historical policy as prior tracks).
/conductor/

# Local matters / operator evidence (never commit client mail)
evidence/
Matters/
matters/

# Accidental drops of large case PSTs at repo root (fixtures/ samples are already tracked)
/INC*.pst
/*.pst
# If you must place a local sample at repo root for smoke, use output/ or Desktop instead.


dist/
*.cdx.json

