# 0102 — Export Oracle Inputs Attest — Plan

> **Placeholder.** Expand before GO.

> **Ledger:** `ledgerful ledger start crates/pst-dedup-cli --category BUGFIX --message "0102 export_oracle export_risk.inputs attest"`

## Phase 0 — Spec expand → Ready

- [ ] Confirm `compare_integrity_counters` runs on normalized vs raw JSON.
- [ ] Lock: strip job-level `inputs` array without deleting `export_risk.inputs`.
- [ ] Expand spec; set **Ready**.

## Phase 1 — Implementation

- [ ] Not started.

## Phase 2 — Finalize

- [ ] `review.md`; conductor **Completed**; ledger commit.
