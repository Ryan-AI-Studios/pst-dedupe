# 0112 — ReviewWindow — Plan

> Phased checklist; map each phase to `spec.md` §7. Execute in `C:\dev\Dedupe`.
> **Ledger:** `ledgerful ledger start crates/dedupe-chrome --category FEATURE --message "0112 three-pane review window"` — commit in the final phase.
>
> Planning tx (this Ready pass): `91a1b6b4-6b40-4160-bade-6802edba5405`.
> Fold-in tx: `bab0df96-f6fd-4fa6-9eab-8200430350ee` (`opencode-review.md` + `agy-review.md`; spec §2.10).

---

## Phase 0 — Precondition / pin gate → DoD-1

- [ ] Re-verify `SCHEMA_VERSION` 39, `get_item`, `apply_codes`, `list_item_codes`, `family_sizes`, `upsert_note`, `upsert_item_privilege`, `privilege_basis::ALL`, `read_cas_prefix` / `cas_len`, `ensure_item_privilege_conn` withhold=1 (do **not** edit it), `is_encrypted_matter` (spec §2.2). Confirm **no** `review_neighbors` / `family_members_thin` yet — this track adds both.
- [ ] Re-verify tauri **2.x stable** + leptos **0.8** (reject 3.x / 0.9-beta / pre-release). Keep `ui/` workspace **exclude**.
- [ ] Confirm CI `chrome-ui` (wasm + trunk **0.21.14**) still present — do not drop it.
- [ ] Confirm `--coding-pane: #e8eef2` still in `ui/styles/tokens.css`.
- [ ] Do **not** vendor `C:\dev\dedupe-frontend`. Do **not** add zpdf. Do **not** implement **0117** queue Bugbot here (header/empty-page/arrow scroll).
- [ ] Do **not** touch unique-pst, `dedupe-desk` Process, or 0111 queue virtualization math.

## Phase 1 — Host commands + neighbors + catalog lock → DoD-2, DoD-3, DoD-4, DoD-5

- [ ] Copy Desk `html_to_review_text` into `crates/dedupe-chrome/src/html_strip.rs` (block-aware; **not** `logical_hash` strip). Twin comment on both files. Port Desk tests `p_tags_do_not_merge_words` (whitespace between Hello/World, not merely `contains`) and `br_and_div_insert_breaks`.
- [ ] `Matter::review_neighbors` in `matter-core` (same FilterSpec WHERE + ORDER BY; sort-key compare so Unreviewed drop-out still yields next; `position` always counted, never 0 solely because the anchor left the filter). Do **not** extend `ReviewListRow`. Do **not** `connection()` SQL in chrome.
- [ ] `Matter::family_members_thin(family_id, limit)` — SQL `LIMIT` 101, columns id/parent/subject/role, **no** `get_family`. Chrome **must not** call `list_family_members`.
- [ ] `review_code_catalog`: `open_matter_read` first; write + `seed_default_codes` only when no active defs (PR #113 Bugbot).
- [ ] `review_document` / `review_document_body` / `review_window_apply` / `review_upsert_note` / `review_upsert_privilege`. Encrypted first; workers; family card via thin helper; body `cas_len` + `read_cas_prefix(2 MiB)` + `from_utf8_lossy`; never innerHTML; never `get_bytes_capped` for the viewer.
- [ ] `review_window_apply` honors `propagate_family` (default false). `review_apply_codes` still **forces** false.
- [ ] Privilege-on sequence **inside** `review_window_apply`: pre-check basis → `apply_codes` → upsert (withhold default false, `include_on_log` true, status asserted) → compensating remove if upsert fails. Do **not** change `ensure_item_privilege_conn`. Do **not** upsert-then-apply.
- [ ] Register new commands in `generate_handler!`. Add `allow-*` for each to `capabilities/default.json`. Rebuild autogenerated permission tomls. No `fs:default`.
- [ ] Unit tests (`tempfile` only):
  - Fixture order: `insert_family` → parent `itm_0000` → children `itm_0001`/`itm_0002`; `ensure_default_review_set` then `in_review=Some(1)`, `review_order`.
  - `family_size==3` via `family_members_thin`; propagate false vs true; Unreviewed neighbors after coding parent (`next_id=itm_0001`, `position != 0`).
  - Privilege without basis → failed, codes unchanged, no claim row. Privilege with basis in **one** `review_window_apply` → final `withhold==false`.
  - Text CAS exact; HTML strip whitespace (not `HelloWorld`); 2 MiB+1 truncated via `cas_len`; missing digest empty.
  - Catalog read-path after seed; queue apply still ignores client `propagate_family=true`.
  - Encrypted kind; missing item `not_found`.
  - Never LocalAppData. Never `open_*` on encrypted.

## Phase 2 — Leptos window → DoD-1, DoD-3, DoD-4

- [ ] Replace `ReviewDocStub` on `/matters/:id/review/:docId` with three panes. Queue route unchanged.
- [ ] Radios ⊥ privilege checkbox + type dropdown. Coding pane class uses `--coding-pane`. Family card always on; apply-to-family off + preview.
- [ ] Native / Text via body command (`<pre class="doc-body">`). Image stub names 0114. Produced `—`.
- [ ] Keyboard §3.8 (`1`/`2`/`3`/`p`/`d`/`Enter`/`[` `]`/`r`/`?`/`Esc`). Overlay says `3` = Needs review. Focus gate on note/select. Ditto = last successful apply snapshot.
- [ ] Skip to document. Plex/paper; no `#ec3013`. CSP unchanged.

## Phase 3 — CI + docs → DoD-5

- [ ] `cargo fmt` / `clippy -D warnings` / `cargo test --workspace` / `cargo test -p dedupe-chrome` / `cargo check -p dedupe-desk` / trunk build `ui/`.
- [ ] CHANGELOG Unreleased. Close `D-0112-review-window`. Leave **0117** Proposed. Leave D-0026-03 open (image half).

## Phase 4 — Finalize → DoD-6

- [ ] Owner HITL: **release** EXE. Synthetic 3-doc family: Enter from queue, `1`+Enter, `p`+type, family off, Image stub, coding pane `#E8EEF2`. INC* waived.
- [ ] `review.md`; `../conductor.md` + `sequencing.md` + `ROADMAP.md`: **0112 Completed**.
- [ ] Commit the ledger transaction.
- [ ] Unblocks **0113** / **0114**. **0115** parked. **0117** still Proposed.

---

## Handoff notes

- Planning-only until the user says **Implement**.
- Single-exe / no-daemon. Process jobs remain `dedupe-desk` until **0116**.
- Privilege coding ⊥ withhold treatment ⊥ responsiveness. Chrome final withhold is the upsert after apply, not the ensure-hook default.
- Do **not** change `ensure_item_privilege_conn` (Desk-shared).
- Control# is `review_order`, not Bates.
- Do **not** implement 0117 queue residuals in this branch unless the user expands that track.
- `conductor/` is gitignored; `git add -f` track files when the owner commits.
- Never commit client PSTs, `output/`, or matter folders with mail.
