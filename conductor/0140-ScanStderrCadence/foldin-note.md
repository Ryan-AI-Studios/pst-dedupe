# Fold-in 0140 — 2026-09-25

Sources: `agy-review.md` + `opencode-review.md` (bundle `AI-review.md`).

Status stays **Ready — not started**.

Folded: verify target `scan_integrity`; DoD-4 retains unique-pst end-of-probe summary; hoist `first_n`; per-file timer re-arm; `-vv` if/else; `scan_progress.rs`; aspose CLI; `TEST_LOCK`; scan-family `-v` help; `on_log` note.
