# 0126 — ProcessChromeVisual — Review

## Scope

Process chrome visual: jobs as a table (job grain; per-row Dupes/NIST/Families/Except. stay `—`), locked builtin checklist, drop-zone copy **PST · ZIP · Purview package · folder**, `\\?\` strip, minus-stack, `process_export_report` 0039 pack. **0122** extract-all Busy / `job_for_orphan` frozen. Schema stays **41**. No fake OST/MBOX/NSRL engines, no Tauri drop, no `workflow_run` picker.

## DoD matrix

| Item | Result | Evidence |
|---|---|---|
| DoD-1 Jobs table | PASS | `<table class="jobs-table">`. Source = `j.kind` (+ child indent). Items from live `progress` else `—`. Dupes/NIST/Families/Except. hardcoded `—`. Pause/Resume/Cancel re-read `progress`. `job_for_orphan` + `is_orphan_running(&job_for_orphan, &progress.get())`. `extract_all_should_start` before `extract_queue.set(q)`. |
| DoD-2 Sources + profile | PASS | Drop copy PST · ZIP · Purview package · folder + hashed on arrival. No OST/MBOX. `strip_extended_path` for `\\?\` / `\\?\UNC\`. One checklist from selected builtin; secondary `<select>`. Near-dup follows live flags. |
| DoD-3 Minus-stack + report | PASS | Minus-stack from live fields. `denist = None` if `cull.never_run` else `Some(culled)` (0 allowed). Unaccounted-for is the 0116 rule. `process_export_report` encrypted/`open_for_read`, no `reject_if_busy`. Capability + autogenerated toml. Exceptions footnote when `exceptions` > grouped sum. `WrapProcess` / TopBar / StatusBar consume `ProcessChromeCtx`. |
| DoD-4 Hygiene | PASS | No new production `unwrap`/`expect`. Schema 41. 0122 Busy tests + `shell_source_locks` green. Plex / `#1b3049`. UI 47; `cargo test -p dedupe-chrome` 121; workspace + clippy `-D warnings`. chrome-ui CI trunk build green. |
| DoD-5 Recorded | PASS | This file; registry **Completed**; `D-0126-process-chrome-visual` and `D-0116-report` closed. Ledger FEATURE committed on the product squash (`914c9c5f`). Owner HITL remaining (release EXE). |

## Gates

| Command | Result |
|---|---|
| `cargo fmt --all --check` | pass |
| `cargo clippy --workspace --all-targets -- -D warnings` | pass |
| `cargo test --manifest-path crates/dedupe-chrome/ui/Cargo.toml` | pass (47) |
| `cargo test -p dedupe-chrome` | pass (121, including denist + export pack + 0122 Busy) |
| `cargo test --workspace` | pass |
| `ledgerful verify` | pass |
| CI (PR **#145**) | fmt, clippy, test, audit, deny, chrome-ui, verify-parity **green**. Bugbot skipping (does not block). |
| Final cross-model gate | **PASS**, no open >low (`review.codex.md`) |

## Reviewer rounds

1. Internal: DoD-1…4 PASS. Easy P3s: accidental `produce.rs` rustfmt (reverted); `export_note` cleared on error; drop-copy / jobs `include_str` tightened.
2. Codex (fresh): **PASS**. No P0–P3.

## HITL (owner)

Release chrome EXE on a synthetic matter: drop copy names PST · ZIP · Purview · folder (not OST/MBOX); source paths without `\\?\`; jobs as a **table**; minus-stack shows **0** (not `—`) for DeNIST/dupes after those stages have run; Unaccounted-for still 0 only on the **0116** extract-success rule; Pause on a live extract-all still does **not** wipe remaining PSTs (**0122**); Download writes a 0039 pack (or shows a fail-closed error). INC* unique-pst is **not** a gate. Codesign is **D-0062-codesign**.

## Residual lows (deferred)

| ID | Item |
|---|---|
| D-0116-drop | Copy only; actual Tauri file drop remains |

## Publish

- Branch: `track/0126-process-chrome-visual`
- PR: **#145**
- Merge SHA: `73c04962ed7b4b3ccb5519f2ea18f6d209056221`
