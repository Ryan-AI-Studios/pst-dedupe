# 0126 — ProcessChromeVisual — Plan

> Phased checklist; map each phase to `spec.md` §7. Execute in `C:\dev\Dedupe`.
> Mark items `- [x]` as completed.
>
> **Ledger (execute):** After owner git-commits this Ready spec/plan if still dirty, `ledgerful ledger start 0126-process-chrome-visual --category FEATURE --message "Process jobs table, locked profile, minus-stack, 0039 download"`

---

## Phase 0 — Precondition / API gate → DoD-4

- [x] Re-verify `SCHEMA_VERSION == 41`. Re-read `ui/src/pages/process.rs`: job `For`, 0122 helpers, chips, extract_all guard, `extract_all_busy_tests` string-locks. Re-read host `ProcessPageResponse` (`denist: None`). Re-read `.process-layout`. Re-read mock `.panes-3-process` (research only).
- [x] Confirm 0122 `extract_all_busy_tests` still name the helpers. Do **not** edit `queue.rs` / `produce.rs` / `queue_window.rs`.
- [x] Confirm last PRs (#144–#141) still have no product findings.

## Phase 1 — Sources + profile + jobs table → DoD-1, DoD-2

- [x] CSS ~ `280px minmax(0,1fr) 320px`. Drop-zone copy: **PST · ZIP · Purview package · folder** + hashed on arrival (not OST/MBOX). `strip_extended_path` + unit test (`\\?\` / `\\?\UNC\`).
- [x] One locked checklist from selected `BuiltinProfileFlags`; secondary builtin `<select>`. Near-dup off unless chosen. Extract/run below checklist.
- [x] Jobs `<table>` with `overflow-x: auto` / nowrap numeric cells: Source = `kind` (+ child indent); Items = live progress for matching `job_id` else `—`; Dupes/NIST/Families/Except. = `—`; Status = live state / %. Same 0122 Pause/Resume/Cancel predicates (re-read `progress` in the row — no one-shot bools). Keep `job_for_orphan` and `is_orphan_running(&job_for_orphan, &progress.get())`.
- [x] `include_str` / existing tests: `extract_all_should_start` still before queue writes; Busy keep-queue strings still present; `extract_all_busy_tests` still pass.

## Phase 2 — Minus-stack + report + shell → DoD-3

- [x] Host: `denist = None` if `ov.cull.never_run` else `Some(ov.cull.culled)`. Optional additive `pdf_needs_ocr` from overview. UI `#[serde(default)]`.
- [x] Right pane minus-stack from live fields; chips may stay as denser duplicates or be replaced — minus-stack is required. Unaccounted-for unchanged.
- [x] `process_export_report`: thin DTOs `ProcessExportReportArgs { root }` / `ProcessExportReportResponse { output_dir, files_written }` (ui `#[serde(default)]`). Wrap `export_matter_report` + `default_matter_report_dir`; `include_pdf: false`. Encrypted / `open_for_read` only — **no** `reject_if_busy`. Wire `build.rs` commands + `lib.rs` `generate_handler!` + `capabilities/default.json` `allow-process-export-report` + `permissions/autogenerated/process_export_report.toml` + `allow_permission_files_exist`-style test. UI: `exporting` disables the button; surface fail-closed errors.
- [x] Exceptions heading count; select group → detail (`code` / `count` / `sample_message`); footnote when `page.exceptions` > sum of `error_groups` counts; no fake retry/vault.
- [x] `wrap_process` creates `ProcessChromeCtx` (`right_label` + `status_left`). **TopBar** (~219) **and** **StatusBar** (~265) consume it. Do not change `PROCESS_FLAG`. Keep `shell_source_locks` literals. Do not steal queue/produce ctx.

## Phase 3 — Verify → DoD-4

- [x] `cargo test --manifest-path crates/dedupe-chrome/ui/Cargo.toml` (must include `extract_all_busy_tests` + `shell_source_locks`)
- [x] `cargo test -p dedupe-chrome` (0122 host Busy if present + process_page denist/unaccounted + export permission test)
- [x] From `crates/dedupe-chrome/ui`: `trunk build --release --config Trunk.toml`

## Phase 4 — Finalize → DoD-5

- [x] `cargo fmt --all --check`
- [x] `cargo clippy --workspace --all-targets -- -D warnings`
- [x] `cargo test --workspace` (or ledgerful verify --scope full)
- [x] CHANGELOG Unreleased sentence.
- [ ] Write `review.md` (commands, HITL table + minus-stack + 0122 Pause + Download).
- [ ] Update `../conductor.md` → **Completed**. Close `D-0126-process-chrome-visual` and `D-0116-report`.
- [ ] Commit the FEATURE ledger transaction.
- [ ] Owner HITL: release EXE.

---

## Handoff notes

- Single-exe / no-daemon. Unique-pst is **not** this page.
- **0122** Busy stays Completed. **0125** produce stays Completed.
- `git add -f` only if `conductor/0126-*` shows **untracked** (placeholder files are already tracked).
- Do not `git add` `.claude` or operator `output/`.
