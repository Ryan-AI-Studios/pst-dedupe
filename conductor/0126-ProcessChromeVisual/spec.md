# 0126 — Process chrome visual (jobs table + minus-stack)

> Structure follows `templates/0000-Description/spec.md`. DoD is §7.
> Phase 0 is **closed** in this file. Do **not** steal **0122** extract-all Busy /
> `busy_retry_pending` / orphan Pause, **0124** `QueueChromeCtx`, **0125**
> Produce canvas / latch, **0119** cancelled-produce, **0120** overlay, or
> **0121** OPT. Do not vendor `C:\dev\dedupe-frontend`. Do not mint a
> BCC-default track. Do not steal **0100–0104**. Do not weaken unaccounted-for.

- **Track ID:** 0126-ProcessChromeVisual
- **Execution repo:** `C:\dev\Dedupe`
- **Governance:** this directory in `C:\dev\Dedupe\conductor\` (track registry: `../conductor.md`)
- **Plan-of-record reference:** Hermes Process workspace. `C:\dev\Dedupe-plan.md` is **absent** (re-verified 2026-09-02); do **not** chase it at execute.
- **Cross-repo contract:** mock at `C:\dev\dedupe-frontend` is **layout research only** (three panes `280 / 1fr / 320`, jobs table, minus-stack). Do not port Archivo, coral, fake `j.reyes.pst` / OST / NSRL 2026.08 / ACME, password vault, or “threading is not inside processing.”
- **Status:** Completed
- **Depends on:** **0116 Completed** (PR **#123** / `727c857`) · **0122 Completed** (PR **#137** / `f1810fe`) · **0123 Completed** (PR **#139** / `fce416e`) · **0125 Completed** (PR **#143** / `1fbc22a`) · schema **v41** (no bump)
- **Spec authored:** 2026-09-02 (placeholder → Ready)
- **Series:** T (mockup chrome fidelity)
>
> **Closes / absorbs:** `D-0126-process-chrome-visual` (this track) and **`D-0116-report`** (0039 CSV pack as Process download). Does **not** close D-0116-drop (copy only), D-0116-workflow, D-0024-01 NSRL RDS, D-0016-05 7z, D-0039-01 PDF, D-0110-deny-unic, D-0062-codesign, D-0125-dead-css / D-0125-pad-fallback.
> **HITL:** owner launches the **release** chrome EXE on a synthetic matter: drop copy names PST · ZIP · Purview · folder (not OST/MBOX); source paths without `\\?\`; jobs as a **table**; minus-stack shows **0** (not `—`) for DeNIST/dupes after those stages have run; Unaccounted-for still 0 only on the **0116** extract-success rule; Pause on a live extract-all still does **not** wipe remaining PSTs (**0122**); Download writes a 0039 pack (or shows a fail-closed error). INC* unique-pst is **not** a gate. Codesign is **D-0062-codesign**.
>
> **Harness fold-in (2026-09-02):** `opencode-review.md` + `agy-review.md`. Drop copy is PST · ZIP · Purview · folder (not OST/MBOX). `process_export_report` wires build.rs + capabilities + autogenerated toml. Encrypted/read-only; no Busy gate. Preserve 0122 `job_for_orphan` string-lock. See §2.9 / §9.
>
> **Owner locks (inherit 0123):** IBM **Plex**, action **ink-navy `#1b3049`**, cool paper, red = privilege / withhold / blocker / exception-risk only. **0123** 46/30 shell stays. `PROCESS_FLAG` already ships the deterministic sentence. This track **fills** the reserved Process right-slot / status-left — it does not restyle the shell.
>
> **Stack lock:** Tauri **2** + Leptos **0.8 CSR** on **stable** Rust. `ui/` stays workspace-excluded. No daemon. No schema bump. One pipeline (`process-runner`). 0122 Busy/orphan helpers unchanged. Default `builtin:standard`. Near-dup stays off unless that builtin is chosen.

---

## 1. Objective

Present **0116**’s honest Process counts in the mockup layout so counsel can read jobs, exceptions, and reconciliation **on one canvas** — not a dump of `kind · state` stacks and chip `—` for stages that already ran.

Silently painting DeNIST/dupes as unknown after cull/dedupe, or copying matter-wide totals onto every job row, is the same honesty class as a silent unique-export drop. Unique-export is unchanged.

---

## 2. Context (read before starting)

### 2.1 Why this track, now

**0125 Completed** (PR **#143** / `1fbc22a`) closed Series T produce. **0122 Completed** locked extract-all Busy and live-row Pause. Live Process (`ui/src/pages/process.rs`) already has **three panes**, `error_groups`, and reconciliation **chips**. The miss is **presentation**: jobs are a stack, profile is four chip-buttons, drop copy and `\\?\` strip are absent, DeNIST is always `None`, minus-stack is chips, Process shell slots are empty, 0039 report is Desk-only.

### 2.2 Live APIs (plan-time 2026-09-02, HEAD `496990c`; re-verify at execute)

| Surface | Fact |
|---|---|
| `schema.rs` | `SCHEMA_VERSION == 41`. **No bump.** |
| `ui/styles/app.css` `.process-layout` | `grid-template-columns: minmax(220px, 1fr) minmax(280px, 1.4fr) minmax(220px, 1fr)`. Three cards already. Mock `.panes-3-process` is `280 / 1fr / 320`. |
| `ui/src/pages/process.rs` | Sources: full `s.path` + `kind · status`; **Add folder** / **Add ZIP / PST**; no drop copy. PST radio + Extract selected/all + profile chips + Run profile. Jobs: `For` of `kind · state` + 0122 Pause/Resume/Cancel. Exceptions: policy sentence + `error_groups` rows. Right: chips Discovered / Exceptions / Review-ready / Still processing / Unaccounted-for / DeNIST `dash(Option)` / Dupes `dash(Option)`. **Open review-ready**. `PROCESS_FLAG` is **not** in the page body (0123 moved it). |
| 0122 helpers (frozen) | `extract_all_should_start`, `is_busy_invoke_err`, `should_clear_queue_on_start_err`, `busy_retry_pending` / `take_busy_retry_fire`, `is_orphan_running`, `snapshot_busy` (`paused` is **not** busy). `extract_all_busy_tests` + `include_str` lock that `extract_all` guards **before** queue writes. Same tests **string-lock** `is_orphan_running(&job_for_orphan, &progress.get())` and forbid `let orphan =` / `let active =` / `let counts = if active` at `For` child create. Table rows must keep those names. |
| Host `src/process.rs` | `ProcessPageResponse`: sources, `pst_inventory`, jobs (`id/kind/state/parent_job_id/error_summary/started_at/finished_at` — **no** `source_id`), `error_groups` (from `list_item_errors_recent(100)`), builtins flags, `discovered` = `ov.totals.top_level_items`, `exceptions` = `ov.errors.total`, `in_review`, `still_processing`, `unaccounted_for` (idle + every inventory PST extracted + no failed-unlogged), `denist: None` **always**, `dupes: Option` from `count_by_dedup_role().duplicate` (None if unique=duplicate=skipped=0), `families` = `ov.totals.families_total` (**unused in UI**). Allowlist kinds: ingest / extract_pst / profile_run / qc / produce. Default profile `builtin:standard`. |
| `Source` | `id/path/kind/status` only. **No** size_bytes, **no** item count. Do **not** call `list_items_for_source` on `process_page` (too heavy). |
| `CaseOverview` already loaded | `cull.never_run` / `cull.culled` / `cull.included`. `ocr.pdf_needs_ocr` (item count, **not** pages). `errors.total` + `errors.by_code`. |
| `ui/src/invoke.rs` `ProcessPageResponse` | Separate WASM mirror. New fields **must** `#[serde(default)]`. |
| `ui/src/app.rs` `wrap_process` | Mounts `<ProcessPage/>` inside `MatterShell` with **no** ctx. |
| `ui/src/shell.rs` | `PROCESS_FLAG` live. Right-slot / status-left empty unless queue or produce ctx. `shell_source_locks` requires `id="queue-goto"`, slot classes, `REVIEW_FLAG`, `PROCESS_FLAG`. |
| 0039 report | `export_matter_report` + `default_matter_report_dir` in `matter-core`. Desk wraps it. **Chrome has no invoke.** PDF ignored (`D-0039-01`). Stamp is `matter_report_YYYYMMDD_HHMMSS` UTC; fail-closed if dir exists. |
| Tauri 2 command wire | New commands need `build.rs` `AppManifest::commands`, `lib.rs` `generate_handler!`, `capabilities/default.json` `allow-*`, and `permissions/autogenerated/<cmd>.toml`. `process_page` is already on that list; `process_export_report` is **not**. Produce tests `allow_permission_files_exist` as the pattern. |
| Ingest kinds | `PackageKind`: `single_pst` / `single_zip` / `purview_package` / `raw_dump` / `unsupported`. **No** OST or MBOX (`detect.rs`). |
| `tauri-plugin-dialog` | Inited. UI pickers use `window.__TAURI__.dialog.open`. |
| Mock `pages/process.rs` | Fake OST, NSRL 2026.08, password vault, 44,446 / 3,744, “threading after processing.” **Do not port fakes.** |
| MS-PST | **N/A this track.** |

### 2.3 Mock + Hermes (research only)

Steal **layout**: drop-zone copy (honest ingest kinds); locked profile checklist; jobs **table**; exceptions master/detail; minus-stack; status-bar job readout.

Do **not** steal: per-source Dupes/NIST as if they were live; OST/MBOX engines; NSRL RDS; credential vault / request-from-custodian; invented ETA; mock “thread is not inside processing” (`builtin:standard` **includes** `thread`).

### 2.4 Plan-time crate pins (re-verify at execute)

| Crate | Plan-time | Rule |
|---|---|---|
| `tauri` | **2** | Reject 3.x. |
| `leptos` / `leptos_router` | **0.8.x** CSR | No major bump. |
| Schema | **41** | No bump. |
| Rust | **stable** | No nightly. |
| trunk | **0.21.14** | Keep. |
| chrome version | **0.2.0-rc.1** | No bump this track. |

### 2.5 Tools / recall

Ran from `C:\dev\Dedupe`:

- `ai-brains preflight --summary` (inited; plan-time **4802**; fold-in **4803**).
- Recall: 0116 unaccounted-for + no OST/NSRL fakes (`f4b60830`); 0122 Busy keep-queue (`bfaa716f`); 0123 `PROCESS_FLAG` (`dc76177b`).
- `ledgerful doctor --json` `readyForPublish: true`; warns phantom-promote, completion-unreachable, sig-pin, sig-version, impact-stale (refreshed this pass).
- Ledger compact: **0 pending / 0 unaudited drift**. Execute starts `0126-process-chrome-visual` **FEATURE** after owner git-commits this plan if still dirty.
- `ledgerful scan --impact` **LOW** (dirty `.claude` junction only — do not `git add`). Hotspots unrelated. Edit surface: `ui/src/pages/process.rs`, `app.css`, `shell.rs` / `app.rs` Process ctx, host `process.rs` + one report command.

### 2.6 What we could not verify

Owner HITL drop-copy + table + minus-stack + 0122 Pause on the release EXE. Execute re-reads `ProcessPageResponse` if anyone added `source_id` on jobs since this pin.

### 2.7 Related deferred (roll)

See §9. Absorb **D-0126** and **D-0116-report**. Remain D-0116-drop (copy only), D-0116-workflow, D-0024-01, D-0016-05, D-0039-01. Decline D-0032-08 / D-0020-01.

### 2.8 Last-PR Cursor comments (2026-09-02)

PRs **#144, #143, #142, #141**. Inline comments **0**. Reviews **0**. Issue comments Bugbot **usage-limit** only. **Decline**.

| Origin | Verdict |
|---|---|
| #143 / #144 ProduceCanvas | **Remain 0125** (Completed). Do not retouch produce. |
| #141 / #142 ReviewQueueChrome | **Remain 0124**. Copy ctx pattern; do not edit queue. |
| #144–#141 usage-limit | **Decline**. |

No new mint. Next free ID **0127**.

### 2.9 Product locks (do not invent at execute)

**0122 Busy frozen.** Keep `extract_all_should_start`, Busy keep-queue, `busy_retry_pending`, `is_orphan_running` derived from **current** `progress`, Pause click = `process_cancel`. Moving jobs into a `<table>` is **not** a license to freeze `orphan`/`active` at `For` child create or to wipe `extract_queue` on Busy.

**Unaccounted-for frozen.** 0 only when idle, every inventory PST has a successful extract (or no PST leaves), and no Failed job lacks `item_errors`. Do not replace this with mock arithmetic.

**Jobs table grain = jobs, not fake sources.** `ProcessJobRow` has no `source_id`. Columns:

| Column | Source |
|---|---|
| Source | `kind` (+ child indent when `parent_job_id` is Some). Optional truncated `error_summary`. Do **not** invent `j.reyes.pst`. |
| Items | If `progress.job_id == row.id`: `completed_count` / `total_hint` (or `—` if no hint). Else `—`. |
| Dupes / NIST / Families / Except. | `—` on **every job row**. Matter-wide numbers live on the **right pane**. Copying `page.dupes` onto each row is a lie. |
| Status | Live `state`; if this row is the busy snapshot, show `%` from completed/total_hint when possible. |

Keep Pause / Resume / Cancel with the **same** 0122 predicates. Preserve `job_for_orphan` and the exact `include_str` call `is_orphan_running(&job_for_orphan, &progress.get())`. Optional jobs-head summary: `N complete · M running` from live `jobs` + snapshot — counts of real rows, not mock 2/1/1. Table CSS: `overflow-x: auto` on the jobs container; `white-space: nowrap` on numeric/status cells.

**DeNIST / dupes honesty.** Host today always sends `denist: None`. This track: `denist = None` while `ov.cull.never_run`; else `Some(ov.cull.culled)` (**0 is allowed**). Label **DeNIST/cull suppressed** — not “NSRL 2026.08”. `dupes` stays `count_by_dedup_role().duplicate`; after dedupe has any roles, show **0** not `—`. Do not import NSRL RDS (**D-0024-01**).

**Minus-stack (right).** Live fields only:

| Line | Source |
|---|---|
| Discovered | `discovered` |
| − DeNIST/cull suppressed | `denist` or `—` if None |
| − duplicate instances | `dupes` or `—` if None |
| − quarantined | `exceptions` |
| Review-ready | `in_review` |
| Unaccounted-for | `unaccounted_for` (first-class) |
| Still processing | `still_processing` |

Do **not** invent a “review masters” identity that disagrees with `in_review`. Do not fake OCR **pages** (`ocr.pdf_needs_ocr` is an **item** count — optional extra stat, never “9,204 pages”). Families = `families` if shown. SHA-256 identity footnote stays.

**Sources (left).** Keep pickers. Add drop-zone **copy** for what ingest actually accepts: **PST · ZIP · Purview package · folder** + “Hashed on arrival.” Do **not** advertise OST / MBOX / loose files (those land `unsupported`). Do **not** wire Tauri drop (**D-0116-drop** remains). Do **not** ingest OST/MBOX/7z. Display names: strip `\\?\` and `\\?\UNC\` → `\\` (helper + unit test). Show `kind · status`. Omit GB / item counts unless a **cheap existing** field appears at execute (plan-time: none). Progress bar only when the live snapshot is an extract for that source’s matching PST name (`extract_current_name`) — otherwise omit, do not fake 68%.

**Locked profile.** One checklist from the **selected** `BuiltinProfileFlags` (default `builtin:standard`). Secondary `<select>` (or equivalent) to switch builtins — not four dominating chip-buttons. Map live flags (classify / office-pdf-ics / OCR / FTS / dedupe / thread / neardup / cull / promote). Near-dup **off** unless chosen. DeNIST checklist row = cull enabled, copy “optional local hash-list (NSRL RDS not this track).” Do **not** invent TZ `America/New_York` or “Standard litigation v3.” Extract selected / Extract all / Run profile **stay** but sit under sources + checklist.

**Exceptions.** Heading includes quarantine count (`exceptions`). Keep “exceptions hold their items” sentence. Select a group → detail shows `code`, `count`, `sample_message`. Retry / exclude / password vault: **honest empty** (“not this track”) unless a host API already exists (plan-time: none). Groups are from recent **100** `item_errors` — if `exceptions` > grouped sum, footnote that.

**Download reconciliation.** New chrome command wrapping `export_matter_report` to `default_matter_report_dir` (`include_pdf: false`, `export_all_jobs: true`). Blocking worker. **Encrypted / `open_for_read` only** — same as `process_page`; do **not** call `reject_if_busy` (export is a WAL snapshot; Download must work while extract runs). Wire the full Tauri 2 surface: `build.rs` commands, `lib.rs` `generate_handler!`, `capabilities/default.json` `allow-process-export-report`, `permissions/autogenerated/process_export_report.toml`, and an `allow_permission_files_exist`-style test. DTOs (do **not** ship `CaseOverview` over IPC):

```text
ProcessExportReportArgs { root: String }
ProcessExportReportResponse { output_dir: String, files_written: Vec<String> }
```

UI mirror: `#[serde(default)]` on new fields. `exporting: RwSignal<bool>` disables the button while in-flight; surface fail-closed “already exists” (second-resolution stamp) on `error` — do not silent-retry into a new second unless the first export already returned. Closes **D-0116-report**. No PDF. No new report schema.

**Shell.** `wrap_process` **creates** `ProcessChromeCtx` (`right_label` + `status_left`, same shape as `ProduceChromeCtx`) **above** `MatterShell`. `ProcessPage` writes labels via `use_context`. **Both** `TopBar` (right-slot match, `shell.rs` ~219) **and** `StatusBar` (status-left match, ~265) must `use_context::<ProcessChromeCtx>()` when queue/produce are absent — providing ctx in `app.rs` alone paints empty slots. Do **not** hoist `extract_queue` / `busy_retry_pending` / Pause. Do not steal `QueueChromeCtx` / `ProduceChromeCtx`. Do not change `PROCESS_FLAG`. Keep `shell_source_locks` literals. No avatar.

**CSS.** Target ~ `280px minmax(0,1fr) 320px`. Plex / `#1b3049` / 0-radius page rules may match 0123 hairlines; no coral.

---

## 3. In scope

`ui/src/pages/process.rs` layout, `ui/styles/app.css` process grid, `shell.rs` / `app.rs` Process ctx + **TopBar and StatusBar** match arms, host `denist` from `ov.cull`, optional additive `pdf_needs_ocr` on the page DTO, `process_export_report` invoke + ui `#[serde(default)]` + `build.rs` / `capabilities/default.json` / autogenerated permission toml. Keep 0122 tests green.

### 3.1 Jobs table + locked profile + drop copy + `\\?\` strip

### 3.2 Minus-stack + 0039 download + Process shell labels

---

## 4. Out of scope (do NOT do here)

- Extract-all Busy / orphan Pause rewrite (**0122**).
- Queue (**0124**). Produce canvas / latch (**0125** / **0119**). Overlay (**0120**). OPT (**0121**).
- `workflow_run` picker (**D-0116-workflow**). Actual Tauri file drop (**D-0116-drop**).
- NSRL RDS import (**D-0024-01**). OST/MBOX engines. 7z (**D-0016-05**).
- Per-source Dupes/NIST SQL. `list_items_for_source` on page load. PDF report (**D-0039-01**).
- Schema bump, BCC-default, Archivo/coral, daemon, unique-pst.

---

## 5. Preconditions & dependencies

- **P1 (blocking):** **0116, 0122, 0123 Completed**. Schema **41**. **0125 Completed** (Series T produce done; copy ctx pattern only).
- *Verified to date:* three-pane CSS; job stack; `denist: None`; chips not minus-stack; no chrome report; wrap_process has no ctx (HEAD `496990c`).

---

## 6. Risks

| Risk | Mitigation |
|---|---|
| Table freeze 0122 one-shot bools | Derive Pause/orphan from current `progress`; keep `extract_all_busy_tests`. |
| Matter-wide dupes on every job row | `—` per job; totals on the right only. |
| NSRL / OST fakes | Copy names PST · ZIP · Purview · folder only; engines stay deferred. |
| Cull never-run shown as 0 | `denist` None → `—` until `!cull.never_run`. |
| Minus-stack disagrees with unaccounted-for | Do not invent a balancing “masters” row. |
| Report clobber | 0039 fail-closed if `output_dir` exists; `exporting` guard; surface the error. |
| Download denied at HITL | Wire build.rs + capabilities + autogenerated toml + permission test. |
| Download blocked during extract | Encrypted/`open_for_read` only; no `reject_if_busy`. |
| WASM DTO skew | Thin `ProcessExportReportResponse`; `#[serde(default)]`. |
| Ctx provided but slots empty | Consume `ProcessChromeCtx` in **both** TopBar and StatusBar. |
| `shell_source_locks` broken | Keep `queue-goto` / slot class / flag literals. |

---

## 7. Definition of Done

Complete only when ALL hold:

- [ ] **DoD-1 — Jobs table:** Jobs render as a table (not `kind · state` stacks). Per-row Dupes/NIST/Families/Except. are `—` unless a live **per-job** field exists (plan-time: none). Pause/Resume/Cancel still follow 0122. `extract_all` still guards before queue writes. `extract_all_busy_tests` still pass (`job_for_orphan` string-lock intact).
- [ ] **DoD-2 — Sources + profile:** Drop-zone copy names **PST · ZIP · Purview package · folder** (not OST/MBOX); pickers stay; display paths strip `\\?\`. One locked checklist from the selected builtin; near-dup off unless chosen. No OST/MBOX/NSRL engines.
- [ ] **DoD-3 — Minus-stack + report:** Reconciliation is a minus-stack. DeNIST/dupes show **0** after those stages have run (`cull.never_run` / empty dedup roles still `—`). Unaccounted-for still uses the 0116 rule. Download writes a 0039 pack and shows the path (capability allow listed). `D-0116-report` closed. Exceptions footnote when `exceptions` > grouped sum.
- [ ] **DoD-4 — Hygiene:** No `unwrap`/`expect` in new production code. No schema bump. 0122 Busy tests + `shell_source_locks` still pass. Process export permission test. Plex / `#1b3049` / no Archivo/coral. `cargo test --manifest-path crates/dedupe-chrome/ui/Cargo.toml` + `cargo test -p dedupe-chrome`. trunk chrome-ui builds (`trunk build --release --config Trunk.toml` from `crates/dedupe-chrome/ui`).
- [x] **DoD-5 — Recorded:** `review.md`; registry **Completed**; CHANGELOG Unreleased; `D-0126-process-chrome-visual` closed; ledger committed (`FEATURE`).

---

## 8. Verification commands (reference)

```powershell
Set-Location C:\dev\Dedupe
cargo test --manifest-path crates/dedupe-chrome/ui/Cargo.toml
cargo test -p dedupe-chrome
cargo fmt --all --check
cargo clippy --workspace --all-targets -- -D warnings
cargo test --workspace
ledgerful verify
# from crates/dedupe-chrome/ui:
# trunk build --release --config Trunk.toml
```

Do **not** `git add` operator PSTs, `output/`, or `.claude`.

---

## 9. Deferred absorb / decline

| ID | Disposition |
|---|---|
| **D-0126-process-chrome-visual** | **Absorb — this track.** |
| **D-0116-report** | **Absorb** — 0039 `export_matter_report` from Process. |
| **D-0116-drop** | Remain (copy only; no Tauri drop). |
| **D-0116-workflow** | Remain (no `workflow_run` picker). |
| **D-0116-process-fold** | Closed in **0116**. |
| **D-0122-process-fold-residuals** | Closed in **0122**. Latch frozen. |
| **D-0123-matter-shell** | Closed in **0123**. Fill Process slots only. |
| **D-0124-review-queue-chrome** | Closed in **0124**. Do not edit queue. |
| **D-0125-produce-canvas** | Closed in **0125**. Do not edit produce. |
| **D-0125-dead-css** / **D-0125-pad-fallback** | Remain (produce residuals). |
| **D-0024-01** | Remain (NSRL RDS). |
| **D-0016-05** | Remain (7z). |
| **D-0039-01** | Remain (PDF report). |
| **D-0110-deny-unic** | Remain (upstream). |
| **D-0032-08** / **D-0020-01** | Decline (operator GUI smoke). |
| **D-0062-codesign** | Remain. |
| Bugbot usage-limit on #141–#144 | **Decline**. |
| Mock OST / NSRL 2026.08 / password vault / ETA / thread-not-in-process | **Decline**. |
| Drop-zone copy advertising OST · MBOX | **Fold** — copy is PST · ZIP · Purview · folder (opencode-M1). |
| opencode-M2 / agy-AGY-126-01 | **Fold** — Tauri build.rs + capabilities + autogenerated toml + permission test. |
| agy-AGY-126-02 | **Fold** — thin `ProcessExportReportArgs` / `Response`. |
| opencode-m1 / agy-AGY-126-05 | **Partial** — `exporting` guard + surface fail-closed; no silent next-second retry. |
| opencode-m2 / agy-AGY-126-06 | **Fold** — encrypted/`open_for_read` only; no Busy. |
| opencode-m3 / agy-AGY-126-04 | **Fold** — TopBar **and** StatusBar consume `ProcessChromeCtx`. |
| agy-AGY-126-03 | **Fold** — keep `job_for_orphan` include_str lock. |
| agy-AGY-126-07 | **Fold** — Phase 2 exceptions footnote. |
| agy-AGY-126-08 | **Fold** — jobs table `overflow-x: auto`. |
| opencode-O1 / O2 | **Already covered** / trivia. |
| BCC-default | Never. |
| Per-source Dupes/NIST without a live per-source API | **Decline** — `—` on job rows. |

---

## 10. Unblocks

Counsel can read Process jobs and reconciliation without decoding a host dump. Series T closes. No further Series T ID unless a new placeholder is minted (**0127+**).
