# 0111 — ReviewQueueFirstPass — Plan

> Phased checklist; map each phase to `spec.md` §7. Execute in `C:\dev\Dedupe`.
> **Ledger:** `ledgerful ledger start crates/dedupe-chrome --category FEATURE --message "0111 virtualized first-pass review queue"` — commit in the final phase.
>
> Planning tx (this Ready pass): `243ca2fa-bb26-4179-a2e3-15998b14bcf2`.
> Fold-in tx: `a8640304-a125-4b28-9346-d7836021309a` (`opencode-review.md` + `agy-review.md`; spec §2.10).

---

## Phase 0 — Precondition / pin gate → DoD-1

- [ ] Re-verify `SCHEMA_VERSION` 39, `list_items_filtered_thin`, `compose_keyword_filter`, `apply_codes`, `list_item_codes`, `list_item_privilege`, `list_saved_searches` / `upsert_saved_search`, `insert_item` `in_review` fields, `is_encrypted_matter` (spec §2.2). Confirm **no** `family_sizes` yet — this track adds it.
- [ ] Re-verify tauri **2.x stable** + leptos **0.8** (reject 3.x / pre-release). Keep `ui/` workspace **exclude**.
- [ ] Confirm CI `chrome-ui` (wasm + trunk **0.21.14**) still present — do not drop it.
- [ ] Do **not** vendor `C:\dev\dedupe-frontend`. Do **not** add zpdf / infinite-scroll-as-corpus / `leptos-struct-table` / radix experimental VirtualList.
- [ ] Do **not** touch unique-pst, `dedupe-desk` Process, or 0110 overview chip map.

## Phase 1 — Host commands + window math → DoD-2, DoD-3, DoD-4, DoD-5

- [ ] Add `matter-search = { path = "../matter-search" }` to `crates/dedupe-chrome/Cargo.toml`.
- [ ] `Matter::family_sizes(&[String]) -> HashMap<String, u64>` in `matter-core` (chunk 500, `COUNT(*)` by `family_id`). Do **not** extend `ReviewListRow`. Do **not** `connection()` SQL in chrome.
- [ ] `queue_window::visible_range` tests: 1000 rows, 640×32, overscan 8 → span ≤ 36; **and** `scroll_top=0`, near-bottom, overscroll clamp.
- [ ] `review_queue_page`: encrypted first; one `open_for_read`; empty keyword = FilterSpec only; keyword = `compose_keyword_filter` with `fts_unavailable` on missing index (assert `kind == "fts_unavailable"`, not generic `is_err()`); `limit` default 500 max 500; `extras: bool` default false.
- [ ] Fill `family_size` via `family_sizes`; codes via `list_item_codes`; first-pass resp/PRIV mapping per spec §3.4. `extras=true` → `list_item_privilege` + `get_item` custodian.
- [ ] `saved_searches_list` / `saved_search_upsert`. `review_code_catalog` (seed_default_codes if empty). `review_codes_preview` in **chrome** from `list_item_codes` + catalog (no matter-core preview API). `review_apply_codes` with `propagate_family=false`, actor `"chrome"`.
- [ ] Register the six commands in `generate_handler!`. Add `allow-*` for each to `capabilities/default.json`. Rebuild so `permissions/autogenerated/*.toml` exist. No `fs:default`.
- [ ] Unit tests (`tempfile` only):
  - 1k fixture: `ensure_default_review_set` id, `in_review=Some(1)`, `review_order=Some(i)`, ids `itm_{i:04}`.
  - page/total; `offset=50` `HashSet::is_disjoint`; empty zeros; insert_source-only total=0; uncoded filter; family_size=3.
  - `extras=false` withhold/custodian empty; `extras=true` fills them.
  - Preview: 3 selected / 1 already privilege → `privilege_would_change=2`; `responsive` and `confidential` → `0`; apply cancel = no write.
  - encrypted kind; `limit=501` failed; FTS-missing → `kind == "fts_unavailable"`.
  - Never LocalAppData. Never `open_*` on encrypted.

## Phase 2 — Leptos queue → DoD-1, DoD-3, DoD-4

- [ ] Replace `ReviewStub` with first-pass queue on `/matters/:id/review`. Add `/matters/:id/review/:docId` stub (“0112”).
- [ ] Default Unreviewed chip. Lead/QC toggle off (first invoke `extras=false`); on → re-invoke `extras=true`. Columns §3.4. Footer uses `total`. Skip to queue.
- [ ] DOM window via `visible_range` (32px rows, overscan 8). Every data row `class="queue-row"`. Do not `<For/>` the full corpus.
- [ ] Keyboard §3.7 (`?`, arrows, Enter, Space, `/`, Esc). Focus gate on keyword/save fields. No ⌘. Do not steal Ctrl+F. 1/2/3 overlay hint only.
- [ ] Bulk bar + privilege confirm using **preview N** (may be &lt; selected). Family indent optional; propagate off.
- [ ] Plex/paper; no `#ec3013`. CSP unchanged.

## Phase 3 — CI + docs → DoD-5

- [ ] `cargo fmt` / `clippy -D warnings` / `cargo test --workspace` / `cargo test -p dedupe-chrome` / `cargo check -p dedupe-desk` / trunk build `ui/`.
- [ ] CHANGELOG Unreleased. Close `D-0111-first-pass-queue`. Leave D-0026-01 open (Desk residual).

## Phase 4 — Finalize → DoD-6

- [ ] Owner HITL: **release** EXE (CSP on bundled assets). Synthetic 1k matter: footer 1000, DOM rows windowed, Unreviewed, `?`, Enter → 0112 stub. INC* waived.
- [ ] `review.md`; `../conductor.md` + `sequencing.md` + `ROADMAP.md`: **0111 Completed**.
- [ ] Commit the ledger transaction.
- [ ] Unblocks **0112**. **0115** stays parked.

---

## Handoff notes

- Planning-only until the user says **Implement**.
- Single-exe / no-daemon. Process jobs remain `dedupe-desk` until **0116**.
- Privilege on the queue is **coding**, not produce treatment.
- Control# is `review_order`, not Bates.
- `conductor/` is gitignored; `git add -f` track files when the owner commits.
- Never commit client PSTs, `output/`, or matter folders with mail.
